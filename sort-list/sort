基于比较

    插入类
        直接插入排序：左边为有序，右边为无序，将无序序列中的数据一个个插入有序序列中
        希尔排序：将相距某个增量的数据组成子序列，在每个子序列中进行插入排序，
                 增量应由大到小，例如4->2->1，增量为1时即全局排序
    
    交换类
        冒泡排序：相邻数据进行比较，如1和2比较，2和3比较，这样比较一趟即可得到一个
                 最值，剩下的元素再重复操作，n个数据经n趟比较可有序
        快速排序：选一个基准数据，将剩下数据与基准比较，分成两边，一边小于基准一边
                 大于基准；对这两边分别重复以上操作直到整个序列有序。
    
    选择类
        简单选择排序：每趟都选择最小（大）的元素放到一边
        堆排序：
                1、将所有数据放进一个堆中
                2、从最后一个数据开始，与双亲结点进行比较，比较大的数据放在双亲结点
                3、这样比较一趟，即可在堆顶得到最大值，输出堆顶值
                4、将最后一个数据换到堆顶，重复2、3操作

    归并类
        二路归并排序：
        多路归并排序：
    

不基于比较
    计数排序
    基数排序
    桶排序
